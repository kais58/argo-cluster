apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs:
    - name: external
      namespace: kube-system
  hostnames: ["grafana.kais58.com"]
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
